//
//  TeamCardView.swift
//  clust
//
//  Created by José Cordeiro on 16/04/23.
//

/*
import SwiftUI
import PassKit

struct TeamCardView: View {
    var body: some View {
            Button(action: {
                if let passURL = Bundle.main.url(forResource: "MyPass", withExtension: "pkpass") {
                    let passData = try! Data(contentsOf: passURL)
                    let pass = try! PKPass(data: passData)
                    let passVC = PKAddPassesViewController(pass: localize)
                    UIApplication.shared.windows.first?.rootViewController?.present(passVC, animated: true, completion: nil)
                }
            }) {
                Text("Add to Wallet")
                    .font(.headline)
                    .foregroundColor(.white)
                    .padding()
                    .background(Color.blue)
                    .cornerRadius(10)
            }
        }
}

struct TeamCardView_Previews: PreviewProvider {
    static var previews: some View {
        TeamCardView()
    }
}

*/
